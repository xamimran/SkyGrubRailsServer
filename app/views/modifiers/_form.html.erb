<%= form_with(model: modifier) do |form| %>
  <% if modifier.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(modifier.errors.count, "error") %> prohibited this modifier from being saved:</h2>

      <ul>
        <% modifier.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
