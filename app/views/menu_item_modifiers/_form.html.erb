<%= form_with(model: menu_item_modifier) do |form| %>
  <% if menu_item_modifier.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(menu_item_modifier.errors.count, "error") %> prohibited this menu_item_modifier from being saved:</h2>

      <ul>
        <% menu_item_modifier.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :menu_item_id, style: "display: block" %>
    <%= form.text_field :menu_item_id %>
  </div>

  <div>
    <%= form.label :modifier_id, style: "display: block" %>
    <%= form.text_field :modifier_id %>
  </div>

  <div>
    <%= form.label :primary, style: "display: block" %>
    <%= form.check_box :primary %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
