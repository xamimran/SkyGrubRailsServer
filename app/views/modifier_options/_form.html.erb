<%= form_with(model: modifier_option) do |form| %>
  <% if modifier_option.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(modifier_option.errors.count, "error") %> prohibited this modifier_option from being saved:</h2>

      <ul>
        <% modifier_option.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :modifier_id, style: "display: block" %>
    <%= form.text_field :modifier_id %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :additional_price, style: "display: block" %>
    <%= form.text_field :additional_price %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
